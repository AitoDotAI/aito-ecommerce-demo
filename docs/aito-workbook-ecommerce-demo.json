{
  "name": "PetNord — Aito Predictive E-commerce Tour",
  "description": "A guided, runnable tour of the predictive capabilities behind the PetNord demo: predictive re-ranking, cross-sell discovery, churn prediction, product filling and price intelligence. Every query below is a verified shape running live against the aito-ecommerce-demo data.",
  "sections": [
    {
      "title": "Start here",
      "widgets": [
        {
          "type": "text",
          "title": "What this workbook is",
          "size": "full",
          "config": {
            "content": "# PetNord — Aito Predictive E-commerce Tour\n\nPetNord is a fictional pet-supplies retailer. This workbook walks through the Aito queries that power its demo app — each one is **runnable**: hit *Run* on any query widget, then edit the query and run again to explore.\n\nThe data is a single denormalised e-commerce dataset:\n\n| Table | Rows about |\n|---|---|\n| `products` | catalog (sku, brand, category, pet_type, price) |\n| `customers` / `customer_months` | customer profiles + monthly snapshots |\n| `orders` / `order_lines` | transactions (line-level, with denormalised customer context) |\n| `monthly_sales` / `price_history` | demand & pricing time series |\n| `reviews` | review text + sentiment + churn label |\n\nNo model training, no feature engineering — Aito predicts directly from the rows already in the database.\n\n**Tour stops:** Explore the data → Predictive re-ranking → Bought Together → Churn prediction → Product Filling → Price Intelligence → Winback (conversion KPI)."
          }
        },
        {
          "type": "metrics",
          "title": "Instance activity",
          "size": "full",
          "config": {
            "displayFields": ["status", "lastRequest", "requests5min", "requests1hr", "memory", "disk"],
            "refreshIntervalSeconds": 30
          }
        }
      ]
    },
    {
      "title": "1 · Explore the data",
      "description": "Plain queries to get a feel for the tables. Edit the `from` and `where` and re-run.",
      "widgets": [
        {
          "type": "query",
          "title": "Browse the product catalog",
          "size": "full",
          "config": {
            "query": { "from": "products", "limit": 10 },
            "endpoint": "query",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "onLoad"
          }
        },
        {
          "type": "query",
          "title": "Customers in one region",
          "size": "medium",
          "config": {
            "query": { "from": "customers", "where": { "region": "helsinki" }, "limit": 10 },
            "endpoint": "query",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        },
        {
          "type": "query",
          "title": "Average order value across all orders",
          "size": "medium",
          "config": {
            "query": { "from": "orders", "aggregate": ["total_eur.$mean", "total_eur.$min", "total_eur.$max"] },
            "endpoint": "aggregate",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    },
    {
      "title": "2 · Predictive re-ranking (Smart Search)",
      "description": "Rank products by P(customer-segment | line containing this product). Products a segment historically buys float to the top.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "A plain text search for `food` returns hundreds of matches in no useful order. Aito re-ranks them for *this customer* using `_recommend`:\n\n- `where` filters the order lines (products whose name matches `food`, for a small-pet household)\n- `recommend: \"product_sku\"` asks Aito to rank candidate SKUs\n- `goal: { customer_segment: \"dog_owner\" }` ranks by **P(dog_owner | this product)** — products dog owners actually buy rise to the top\n- `basedOn` curates which product features feed the prior, trimming ~13% off latency\n\nChange `customer_segment` to `cat_owner` and re-run — the ranking flips to cat products.\n\n**Why segment, not a purchase/click KPI?** `order_lines` records only purchases — there's no negative class to learn a `P(purchase)` from, and its one outcome field (`returned`) is uniform at ~3%, so a `goal: { returned: false }` collapses to baseline. Here, segment affinity *is* the purchase-propensity signal: by Bayes, `P(dog_owner | product)` is highest for the products dog owners disproportionately buy. For a recommendation conditioned on a true conversion KPI, see the **Winback** section below — it has a real `responded` outcome to rank on."
          }
        },
        {
          "type": "query",
          "title": "Re-rank 'food' results for a dog-owner household",
          "size": "full",
          "config": {
            "query": {
              "from": "order_lines",
              "where": {
                "product_sku.name": { "$match": "food" },
                "customer_pet_size": "small"
              },
              "recommend": "product_sku",
              "goal": { "customer_segment": "dog_owner" },
              "basedOn": ["pet_type", "brand", "dietary", "category"],
              "limit": 10
            },
            "endpoint": "recommend",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    },
    {
      "title": "3 · Bought Together (cross-sell)",
      "description": "Statistical co-occurrence between order contents, surfaced with `_relate`.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "`orders.line_categories` is a denormalised text column listing each order's lines as `<pet>_<category>` tokens. `_relate` returns the **lift** of token co-occurrence within an order:\n\n- `lift > 1` → bought together more often than chance (cross-sell)\n- `lift < 1` → rarely bought together (protective / substitute)\n\nRead the `related.line_categories.$has` column against `lift`. Swap `dog_dryfood` for `cat_wetfood` to explore a different basket."
          }
        },
        {
          "type": "query",
          "title": "What gets bought with dog dry-food?",
          "size": "full",
          "config": {
            "query": {
              "from": "orders",
              "where": { "line_categories": { "$match": "dog_dryfood" } },
              "relate": "line_categories",
              "limit": 12
            },
            "endpoint": "relate",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    },
    {
      "title": "4 · Churn prediction",
      "description": "Predict whether a customer will churn in the next 3 months — and measure how accurate the prediction is.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "`customer_months` holds a monthly snapshot per customer, labelled `churned_in_3_months` (true iff they placed no order in the following 3 months). `_predict` returns P(true) and P(false) for a customer described by `where` — no model training needed.\n\nThe profile below describes a small-dog owner in Helsinki who just left a negative, low-rated review — a classic at-risk signal. Edit the fields and re-run to see the probability move.\n\nThe **accuracy widget** below holds out a random sample, hides the label, predicts it from the same features, and compares against ground truth — Aito's built-in cross-validation."
          }
        },
        {
          "type": "query",
          "title": "Will this customer churn?",
          "size": "full",
          "config": {
            "query": {
              "from": "customer_months",
              "where": {
                "segment": "dog_owner",
                "pet_size": "small",
                "region": "helsinki",
                "latest_sentiment": "negative",
                "latest_rating": 2
              },
              "predict": "churned_in_3_months",
              "limit": 2
            },
            "endpoint": "predict",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        },
        {
          "type": "evaluate",
          "title": "How accurate is the churn prediction?",
          "size": "full",
          "config": {
            "queryType": "predict",
            "query": {
              "from": "customer_months",
              "predict": "churned_in_3_months",
              "where": {
                "segment": { "$get": "segment" },
                "region": { "$get": "region" },
                "tenure_months_at_month": { "$get": "tenure_months_at_month" },
                "visits": { "$get": "visits" },
                "purchases": { "$get": "purchases" },
                "spent_eur": { "$get": "spent_eur" }
              }
            },
            "testSelection": { "type": "random", "percentage": 20 }
          }
        }
      ]
    },
    {
      "title": "5 · Product Filling",
      "description": "Predict missing product attributes from the product name and brand.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "A new product arrives with only a name and brand. Aito fills the rest of the catalog record by predicting each field from the products it already knows. Here we predict `category` from the name token `food` + brand — change `predict` to `pet_type`, `dietary`, or `tax_class` to fill a different field, or edit the `$match` name to describe a different product."
          }
        },
        {
          "type": "query",
          "title": "Predict a product's category from its name",
          "size": "full",
          "config": {
            "query": {
              "from": "products",
              "where": {
                "name": { "$match": "Royal Canin Chicken Dog Food" },
                "brand": "Royal Canin"
              },
              "predict": "category",
              "limit": 3
            },
            "endpoint": "predict",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    },
    {
      "title": "6 · Price Intelligence",
      "description": "Server-side price statistics per SKU with `_aggregate`.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "`_aggregate` computes mean / min / max over `price_history` for one SKU, server-side. `$mean` also returns variance, standard deviation and standard error in the same response — useful for spotting price outliers and discount depth. Swap the `product_sku` to inspect a different product's price band."
          }
        },
        {
          "type": "query",
          "title": "Price band for one SKU",
          "size": "full",
          "config": {
            "query": {
              "from": "price_history",
              "where": { "product_sku": "SKU-PT-0001" },
              "aggregate": ["price_eur.$mean", "price_eur.$min", "price_eur.$max"]
            },
            "endpoint": "aggregate",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    },
    {
      "title": "7 · Winback — recommend by conversion probability",
      "description": "The textbook recommendation shape: rank products by the probability the customer actually converts.",
      "widgets": [
        {
          "type": "text",
          "title": "How it works",
          "size": "full",
          "config": {
            "content": "Unlike `order_lines`, the `winback_campaigns` table records a real **outcome**: each row is a campaign send with `responded: true|false`. That gives Aito a conversion KPI to optimise directly.\n\n- `recommend: \"product_sku\"` over `winback_campaigns`\n- `goal: { responded: true }` ranks candidates by **P(responded = true | this product, this customer's context)**\n- `where` pins the customer's profile (segment, lifestyle, recency since last order)\n\nThe app's Winback view then multiplies this response probability by a predicted `order_value_eur` to estimate €-recoverable revenue per send. The `_predict` widget below reads the *absolute* response probability for one product context (`_recommend`'s `$p` is normalised against goal-positives, so use `_predict` when you need the calibrated number)."
          }
        },
        {
          "type": "query",
          "title": "Best winback products for a lapsed premium dog-owner",
          "size": "full",
          "config": {
            "query": {
              "from": "winback_campaigns",
              "where": {
                "customer_segment": "dog_owner",
                "customer_lifestyle": "premium",
                "recency_bucket": "90-180d"
              },
              "recommend": "product_sku",
              "goal": { "responded": true },
              "basedOn": ["pet_type", "category", "brand"],
              "limit": 8
            },
            "endpoint": "recommend",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        },
        {
          "type": "query",
          "title": "Calibrated response probability for one product context",
          "size": "full",
          "config": {
            "query": {
              "from": "winback_campaigns",
              "where": {
                "customer_segment": "dog_owner",
                "customer_lifestyle": "premium",
                "recency_bucket": "90-180d",
                "product_category": "dry-food"
              },
              "predict": "responded",
              "limit": 2
            },
            "endpoint": "predict",
            "displayMode": "playground",
            "outputFormat": "table",
            "runPolicy": "manual"
          }
        }
      ]
    }
  ]
}
